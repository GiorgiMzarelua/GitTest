import acm.program.ConsoleProgram;

public class quickSort extends ConsoleProgram{
    public void run(){
    	int n = readInt("Enter the quantity of numbers: ");
    	int[] array = new int[n];
    	for(int i = 0; i < n; i++){
    		array[i] = readInt("Enter the " + (i + 1) + "-th number of the list: ");
    	}
    	int pointer = array[n-1];
    	int greater;
    	int smaller;
    	for(int i = 0; i < n; i++){
    		
    	}
    }
}
